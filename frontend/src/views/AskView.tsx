export function AskView() {
  return (
    <div className="mx-auto max-w-[70ch] px-6 py-16">
      <p className="text-body leading-relaxed text-text-muted">
        The reasoning console — ask an open question and get a cited,
        disagreement-aware answer synthesised from the project's verified facts —
        is not enabled yet. It arrives in a later phase and will surface any
        relevant contradiction inline rather than resolving it silently.
      </p>
    </div>
  );
}
